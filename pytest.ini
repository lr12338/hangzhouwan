# -*- coding: utf-8 -*-
# pytest 收集范围限定为 tests/（单元+集成），与 tests/run_tests.py 一致。
# tools/ 下的 *_test.py 为独立集成脚本（如 business_sidecar_client_test.py 需启动
# sidecar 进程并通过 main() 注入 socket），不属于 pytest 单元测试，不纳入自动收集。
[pytest]
testpaths = tests
