# /run/hangzhouwan 由 tmpfiles.d 统一管理，不由单个 systemd 服务独占生命周期。
# Business 与 Video 共享此目录存放 Unix Domain Socket：
#   business.sock        (业务增强 Sidecar)
#   video-health.sock    (Video 结构化健康接口)
# 类型 d：不存在时创建，不随服务停止删除，保证重启任一服务不丢失另一方的 Socket。
d /run/hangzhouwan 0755 linaro linaro -
